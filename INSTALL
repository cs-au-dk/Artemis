0. Please ensure you have a working installation of Qt 4.8 or
higher. If this is not the case, a copy can be downloaded by running

   make qt-checkout   

Enter the directory, "./configure" and "make".

1. Double check that qmake is on your path and then build the
instrumented webkit by running
   
   make webkit-build[-debug|-minimal]

2. Install the newly built QtWebKit port in your existing QT
installation by running

   make webkit-install-into-qt

3. Build artemis!

   make artemis

